# Example: Doc-Writer Team

A scanner files several "write me a section" tasks, then a fleet of
builders pick them off the kanban in parallel. The scanner sends
follow-up notes to each builder via the inbox transport.

Run with:

```bash
python examples/doc_writer_team/run.py
```
